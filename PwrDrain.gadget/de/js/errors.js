var ErrMsg = {
  noMeters: function () { return 'Keine geeigneten Leistungsmesser im System gefunden. Dies kann daran liegen, dass Sie kein batteriebetriebenes Gerät benutzen.'; },
  noStyle: function (name) { return 'Stil "' + name + '" nicht gefunden.'; },
  noBackground: function (name) { return 'Stil "' + name + '" enthält keinen background.'; },
  noHand: function (name) { return 'Stil "' + name + '" enthält kein hand.'; }
};
